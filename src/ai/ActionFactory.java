package ai;

import java.util.List;

/**
 * Class to generate actions given a state.
 */
public class ActionFactory {
	public List<Action> getActions(State s) { // should also depend on which player?
		
		// for every queen (of a player?)
			// for every valid queen move
				// for every valid arrow shot
					// actions.add(   )
		
		return null; // return the list of possible actions for the state
	}
}
